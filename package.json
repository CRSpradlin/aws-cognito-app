{
  "name": "aws-cognito-application",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "mkdir ./modules/lambda/opt; rm -rf ./modules/lambda/opt/*.js; cp ./modules/services/code/*.js ./modules/lambda/opt",
    "test": "jest",
    "deploy": "zsh ./scripts/deploy.sh",
    "build": "zsh ./scripts/build.sh",
    "destroy": "cd aws; terraform destroy -var-file=variables.tfvars",
    "apply": "cd aws; terraform apply -var-file=variables.tfvars",
    "update-modules": "cd aws; terraform init;"
  },
  "author": "crspradlin",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.2.0"
  },
  "dependencies": {
    "aws-sdk": "^2.991.0",
    "eslint": "^7.32.0"
  }
}
